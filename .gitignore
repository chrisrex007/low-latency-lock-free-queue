# Compiled test and benchmark binaries
test_spsc_queue
test_mutex_queue
test_mpmc_queue
test_*_tsan
test_*_asan
benchmark

# Transient test log
output.log

# Locally regenerated by `make run-bench` / `make plot`. The published results
# live in server_results/ and ARE committed — the leading slash keeps these
# rules to the repo root so server_results/*.png is not swept up with them.
/bench_results.csv
/throughput_vs_msgsize.png
/throughput_vs_threads.png

# Local AI-assistant context, not part of the project
CLAUDE.md

# Personal study / interview-prep notes, not part of the project
doc.md
